#!/usr/bin/python3
def number_keys(a_dictionary):
    get = 0
    for i in list(a_dictionary.keys()):
        get += 1
    return get
    """
    for i in range(len(a_dictionary)):
        pass
    return (i + 1)
    """
